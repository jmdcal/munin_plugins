~~~~~~~~~
LogFormat
~~~~~~~~~

Every virtual host you want to monitor needs to use combined_format2 defined in this way:

LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\" [[%D]] <<%{Content-Type}o>> {{bal_%{BALANCER_WORKER_ROUTE}e}}" combined_format2